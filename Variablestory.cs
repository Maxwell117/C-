using system;
namespace VariablesExercise
{
    public class Program
    {
        static void Main(string[] args)
        {
            console.readline(s);
           console.readline(age);
            console.readline(dd);
            console.readline(ss);
            string s;
            string ss;
            int age;
            string place;
 
            Consule.writeline("Please enter your name,age,a decimal and your best friend's name");
            s= console.readline();
           age=console.readline();
            place=console.readline();
           ss= console.readline();
            
            Consule.writeline(s+" age" + age + "met "+ss+"at the "+place);
           
        }
    }
}
